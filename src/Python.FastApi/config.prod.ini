# 프로덕션 환경 설정
[runtime]
PARALLEL_WORKERS=8
BATCH_SIZE=16
QUEUE_MAX=2000

[crawl]
DEPTH=3
RATE_LIMIT_PER_DOMAIN=5
MAX_PAGES=5000

[llm]
PROVIDER=openai
MODEL_CHAT=gpt-4
MODEL_EMBED=text-embedding-3-large

[database]
POOL_SIZE=20
MAX_OVERFLOW=40

[logging]
LEVEL=INFO